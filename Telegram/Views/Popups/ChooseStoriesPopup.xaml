<controls:ContentPopup x:Class="Telegram.Views.Popups.ChooseStoriesPopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:common="using:Telegram.Common"
                       xmlns:controls="using:Telegram.Controls"
                       xmlns:cells="using:Telegram.Controls.Cells"
                       xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:not_win="http://uno.ui/not_win"
                       mc:Ignorable="d not_win"
                       VerticalContentAlignment="Stretch"
                       Padding="0,24,0,0">

    <Grid>
        <GridView x:Name="ScrollingHost"
                  SelectionMode="Multiple"
                  ChoosingItemContainer="OnChoosingItemContainer"
                  ContainerContentChanging="OnContainerContentChanging"
                  common:FluidGridView.MaxLength="1008"
                  Padding="22,0,22,22">
            <common:FluidGridView.Triggers>
                <common:FluidGridViewTrigger RowsOrColumns="3" />
            </common:FluidGridView.Triggers>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <!--
                        ItemsWrapGrid is [NotImplemented] on Uno Skia (no Measure/Arrange at all);
                        VariableSizedWrapGrid is the proven substitute (profile Media/Gifts/
                        Animations tabs, CommonStyles' implicit default). No Header/Footer/grouping
                        on this GridView, so the panel swap alone is enough here.
                    -->
                    <win:ItemsWrapGrid Orientation="Horizontal" />
                    <not_win:VariableSizedWrapGrid Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem"
                       BasedOn="{StaticResource DefaultGridViewItemStyle}">
                    <Setter Property="Margin"
                            Value="2" />
                    <Setter Property="Padding"
                            Value="0" />
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch" />
                    <Setter Property="VerticalContentAlignment"
                            Value="Stretch" />
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <cells:StoryCell />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <controls:ScrollViewerScrim Background="{ThemeResource ContentDialogTopOverlaySolid}"
                                    ScrollingHost="{x:Bind ScrollingHost}"
                                    BottomInset="0" />
    </Grid>
</controls:ContentPopup>